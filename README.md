Simple budgeting tool of keeping track of who owes you money.

How to start:
npm install //installs dependencies
npm run build //starts webpack
npm run run //starts the server

Set to run on port 5000
